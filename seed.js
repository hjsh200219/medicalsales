const { PrismaClient } = require(\"@prisma/client\"); const prisma = new PrismaClient(); async function seed() { try { const years = []; for (let year = 1990; year <= 2024; year++) { years.push({ year, institution_count: Math.floor(Math.random() * 1000) + 100 }); } await prisma.institution_count_year.deleteMany({}); await prisma.institution_count_year.createMany({ data: years }); console.log(\"테스트 데이터 생성 완료\"); } catch (error) { console.error(\"오류 발생:\", error); } finally { await prisma.$disconnect(); } } seed();
